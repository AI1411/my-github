# M3-013/014: GraphQL Inbox + SQLite Pool Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `graphql_client` crate で inbox query の Rust 型生成と呼び出し関数を実装（#48）、`r2d2` + `rusqlite` でアプリデータディレクトリに `pulse.db` の接続プールを初期化する（#49）。

**Architecture:**
- `src-tauri/src/github/graphql/mod.rs` に `#[derive(GraphQLQuery)]` で `InboxQuery` 型を生成し、`GithubClient` で `/graphql` エンドポイントへ POST する `fetch_inbox()` 関数を配置する。既存の `src-tauri/src/github/graphql/queries/inbox.graphql` を `query_path` として参照する。
- `src-tauri/src/db/mod.rs` に `init_pool(path)` と `pulse_db_path(app)` を分離して配置。前者は純粋関数でテスト容易、後者は `AppHandle` から `app_data_dir` を解決する薄いラッパ。

**Tech Stack:** Rust 1.82+, `graphql_client` 0.14, `rusqlite` 0.31 (bundled), `r2d2` 0.8, `r2d2_sqlite` 0.24, `tauri` 2, `tempfile`（dev-dep として追加）

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `src-tauri/src/github/graphql/schema.graphql` | Create | GitHub GraphQL schema（graphql_client の `schema_path` 用） |
| `src-tauri/src/github/graphql/mod.rs` | Create | `InboxQuery` (GraphQLQuery derive) + `fetch_inbox` 関数 + `GraphqlError` + tests |
| `src-tauri/src/github/mod.rs` | Modify | `pub mod graphql;` を追加 |
| `src-tauri/src/db/mod.rs` | Create | `init_pool`, `pulse_db_path`, `DbError`, `SqlitePool` 型エイリアス + tests |
| `src-tauri/src/lib.rs` | Modify | `pub mod db;` を追加 |
| `src-tauri/Cargo.toml` | Modify | `tempfile` を `[dev-dependencies]` に追加 |

---

## Branch Strategy

- まず `main` を最新化する。
- **Issue #48 (M3-013)** 用に `main` から `issue-48` ブランチを作成して実装・コミット・プッシュ。
- その後 `main` に戻り（または pull し直し）、**Issue #49 (M3-014)** 用に `main` から `issue-49` ブランチを作成して実装・コミット・プッシュ。
- ブランチは順番に作成（並列 PR は M3-013 の graphql モジュール確定後の方が安全）。

---

## Preamble: update main

- [x] **Step 0-1: 現在のブランチをクリーンアップし main を最新化**

```bash
git checkout main
git pull --ff-only origin main
```

---

# Issue #48 (M3-013): GraphQL Inbox Query

### Task 1: issue-48 ブランチを作成

**Files:** なし（ブランチ操作のみ）

- [x] **Step 1-1: ブランチ作成**

```bash
git checkout -b issue-48
```

---

### Task 2: GitHub GraphQL schema をダウンロード

**Files:**
- Create: `src-tauri/src/github/graphql/schema.graphql`

- [x] **Step 2-1: GitHub の公開 GraphQL schema を取得**

Run:
```bash
curl -fsSL https://docs.github.com/public/fpt/schema.docs.graphql \
  -o src-tauri/src/github/graphql/schema.graphql
```

Expected: `src-tauri/src/github/graphql/schema.graphql` が作成される（~2-3MB）。

- [x] **Step 2-2: ダウンロード結果を検証**

Run:
```bash
head -1 src-tauri/src/github/graphql/schema.graphql
wc -l src-tauri/src/github/graphql/schema.graphql
```

Expected: 先頭が `"""` か `directive` か `schema {` か `scalar` で始まり、行数が 10000 行以上。`<!DOCTYPE html>` 等の HTML が返ってきていたら URL 誤り。

- [x] **Step 2-3: コミット**

```bash
git add src-tauri/src/github/graphql/schema.graphql
git commit -m "chore(m3-013): vendor GitHub GraphQL schema for graphql_client codegen"
```

---

### Task 3: `graphql_client` で `InboxQuery` 型を生成するスケルトンを追加（失敗テストから）

**Files:**
- Create: `src-tauri/src/github/graphql/mod.rs`
- Modify: `src-tauri/src/github/mod.rs`

- [x] **Step 3-1: まず失敗するテストのみを含む `mod.rs` を作成**

`src-tauri/src/github/graphql/mod.rs` を作成:

```rust
use graphql_client::GraphQLQuery;

#[derive(GraphQLQuery)]
#[graphql(
    schema_path = "src/github/graphql/schema.graphql",
    query_path = "src/github/graphql/queries/inbox.graphql",
    response_derives = "Debug, Clone, PartialEq"
)]
pub struct InboxQuery;

#[cfg(test)]
mod tests {
    use super::*;
    use graphql_client::GraphQLQuery;

    #[test]
    fn build_query_serializes_variables() {
        let body = InboxQuery::build_query(inbox_query::Variables { first: Some(25) });
        assert_eq!(body.operation_name, "InboxQuery");
        assert!(body.query.contains("reviewRequests"));
        assert!(body.query.contains("mentions"));
        assert!(body.query.contains("assignedIssues"));
        let vars = serde_json::to_value(&body.variables).unwrap();
        assert_eq!(vars["first"], 25);
    }
}
```

- [x] **Step 3-2: `github/mod.rs` に `pub mod graphql;` を追加**

`src-tauri/src/github/mod.rs` の内容を以下に変更（既存の行は残しつつ 1 行追加）:

```rust
pub mod client;
pub mod graphql;
pub mod rest;
pub mod types;
```

- [x] **Step 3-3: ビルド&テスト実行（型生成と query 構築が通ることを確認）**

Run:
```bash
cargo test -p tauri-app --lib github::graphql::tests::build_query_serializes_variables -- --nocapture
```

Expected: PASS。生成された `inbox_query::Variables` と `InboxQuery::build_query` が動作する。

（もし schema 不整合で落ちる場合は Step 2-2 を再実行して schema が HTML 等になっていないか確認。）

- [x] **Step 3-4: コミット**

```bash
git add src-tauri/src/github/graphql/mod.rs src-tauri/src/github/mod.rs
git commit -m "feat(m3-013): generate InboxQuery types via graphql_client"
```

---

### Task 4: レスポンス JSON のデシリアライズテスト（TDD: 先に失敗させる）

**Files:**
- Modify: `src-tauri/src/github/graphql/mod.rs`

- [x] **Step 4-1: `response_derives` に `Serialize, Deserialize` を追加（失敗テスト前の下準備）**

`src-tauri/src/github/graphql/mod.rs` の `#[graphql(...)]` 属性を以下に更新:

```rust
#[derive(GraphQLQuery)]
#[graphql(
    schema_path = "src/github/graphql/schema.graphql",
    query_path = "src/github/graphql/queries/inbox.graphql",
    response_derives = "Debug, Clone, PartialEq, Serialize, Deserialize"
)]
pub struct InboxQuery;
```

そして先頭に use を追加:

```rust
use graphql_client::GraphQLQuery;
use serde::{Deserialize, Serialize};
```

（`Serialize, Deserialize` は生成コードが参照するので import で可視化する。`#[allow(unused_imports)]` は不要。直接使わず derive 側で参照される場合、`pub use` でも OK だが単純に use で問題なし。）

- [x] **Step 4-2: レスポンスのデシリアライズテストを追加（失敗確認）**

`src-tauri/src/github/graphql/mod.rs` の `mod tests` 内に以下を追記:

```rust
    #[test]
    fn deserializes_inbox_response_data() {
        let json = serde_json::json!({
            "reviewRequests": {
                "issueCount": 1,
                "nodes": [
                    {
                        "__typename": "PullRequest",
                        "id": "PR_kw1",
                        "number": 42,
                        "title": "Add feature",
                        "url": "https://github.com/o/r/pull/42",
                        "createdAt": "2026-04-01T00:00:00Z",
                        "updatedAt": "2026-04-02T00:00:00Z",
                        "isDraft": false,
                        "state": "OPEN",
                        "repository": { "nameWithOwner": "o/r" },
                        "author": { "login": "alice", "avatarUrl": "https://x/y.png" }
                    }
                ]
            },
            "mentions": { "issueCount": 0, "nodes": [] },
            "assignedIssues": { "issueCount": 0, "nodes": [] },
            "rateLimit": {
                "limit": 5000,
                "cost": 1,
                "remaining": 4999,
                "resetAt": "2026-04-21T01:00:00Z"
            }
        });
        let data: inbox_query::ResponseData = serde_json::from_value(json).unwrap();
        assert_eq!(data.review_requests.issue_count, 1);
        assert_eq!(data.mentions.issue_count, 0);
        assert_eq!(data.assigned_issues.issue_count, 0);
        let rl = data.rate_limit.unwrap();
        assert_eq!(rl.remaining, 4999);
    }
```

- [x] **Step 4-3: テストを実行してまず型の整合を確認**

Run:
```bash
cargo test -p tauri-app --lib github::graphql -- --nocapture
```

Expected: PASS。生成されたフィールド名は GraphQL の camelCase → Rust の snake_case（`review_requests`, `issue_count`, `rate_limit` 等）に自動変換される。

もし生成フィールド名がズレた場合は `cargo expand` またはコンパイラエラーから正しいパスを読み取って assertion を修正する。

- [x] **Step 4-4: コミット**

```bash
git add src-tauri/src/github/graphql/mod.rs
git commit -m "test(m3-013): verify InboxQuery response deserialization"
```

---

### Task 5: `fetch_inbox` 関数を実装

**Files:**
- Modify: `src-tauri/src/github/graphql/mod.rs`
- Modify: `src-tauri/src/github/client.rs` (POST 用 helper 追加)

- [x] **Step 5-1: `GithubClient` に `post_graphql` 用の汎用 POST helper を追加**

`src-tauri/src/github/client.rs` の `impl GithubClient` ブロック末尾（`pub fn get` の下）に追加:

```rust
    pub fn post(&self, path: &str) -> reqwest::RequestBuilder {
        let url = format!("{}{}", GITHUB_API_BASE, path);
        self.inner
            .post(url)
            .header("Authorization", format!("Bearer {}", self.token))
            .header("User-Agent", USER_AGENT)
            .header("Accept", "application/vnd.github+json")
    }
```

さらに `#[cfg(test)] mod tests` に以下を追加:

```rust
    #[test]
    fn post_builds_request_with_auth_header() {
        let client = GithubClient::new("gho_mytoken");
        let req = client.post("/graphql").build().unwrap();
        assert_eq!(req.method(), reqwest::Method::POST);
        let auth = req
            .headers()
            .get("Authorization")
            .unwrap()
            .to_str()
            .unwrap();
        assert_eq!(auth, "Bearer gho_mytoken");
    }
```

- [x] **Step 5-2: client.rs のテスト実行**

Run:
```bash
cargo test -p tauri-app --lib github::client::tests::post_builds_request_with_auth_header -- --nocapture
```

Expected: PASS。

- [x] **Step 5-3: `fetch_inbox` のシグネチャと最小実装**

`src-tauri/src/github/graphql/mod.rs` 全体を以下に更新:

```rust
use graphql_client::{GraphQLQuery, QueryBody, Response};
use serde::{Deserialize, Serialize};
use thiserror::Error;

use crate::github::client::{ClientError, GithubClient};

#[derive(GraphQLQuery)]
#[graphql(
    schema_path = "src/github/graphql/schema.graphql",
    query_path = "src/github/graphql/queries/inbox.graphql",
    response_derives = "Debug, Clone, PartialEq, Serialize, Deserialize"
)]
pub struct InboxQuery;

#[derive(Debug, Error)]
pub enum GraphqlError {
    #[error(transparent)]
    Client(#[from] ClientError),
    #[error("GraphQL errors: {0}")]
    Query(String),
    #[error("Missing data in GraphQL response")]
    MissingData,
}

pub async fn fetch_inbox(
    client: &GithubClient,
    first: i64,
) -> Result<inbox_query::ResponseData, GraphqlError> {
    let body: QueryBody<inbox_query::Variables> =
        InboxQuery::build_query(inbox_query::Variables { first: Some(first) });

    let resp = client
        .post("/graphql")
        .json(&body)
        .send()
        .await
        .map_err(ClientError::from)?;

    let status = resp.status();
    if !status.is_success() {
        let message = resp.text().await.unwrap_or_default();
        return Err(GraphqlError::Client(ClientError::Api {
            status: status.as_u16(),
            message,
        }));
    }

    let parsed: Response<inbox_query::ResponseData> =
        resp.json().await.map_err(ClientError::from)?;

    if let Some(errors) = parsed.errors {
        if !errors.is_empty() {
            let joined = errors
                .iter()
                .map(|e| e.message.clone())
                .collect::<Vec<_>>()
                .join("; ");
            return Err(GraphqlError::Query(joined));
        }
    }

    parsed.data.ok_or(GraphqlError::MissingData)
}

#[cfg(test)]
mod tests {
    use super::*;
    use graphql_client::GraphQLQuery;

    #[test]
    fn build_query_serializes_variables() {
        let body = InboxQuery::build_query(inbox_query::Variables { first: Some(25) });
        assert_eq!(body.operation_name, "InboxQuery");
        assert!(body.query.contains("reviewRequests"));
        assert!(body.query.contains("mentions"));
        assert!(body.query.contains("assignedIssues"));
        let vars = serde_json::to_value(&body.variables).unwrap();
        assert_eq!(vars["first"], 25);
    }

    #[test]
    fn deserializes_inbox_response_data() {
        let json = serde_json::json!({
            "reviewRequests": {
                "issueCount": 1,
                "nodes": [
                    {
                        "__typename": "PullRequest",
                        "id": "PR_kw1",
                        "number": 42,
                        "title": "Add feature",
                        "url": "https://github.com/o/r/pull/42",
                        "createdAt": "2026-04-01T00:00:00Z",
                        "updatedAt": "2026-04-02T00:00:00Z",
                        "isDraft": false,
                        "state": "OPEN",
                        "repository": { "nameWithOwner": "o/r" },
                        "author": { "login": "alice", "avatarUrl": "https://x/y.png" }
                    }
                ]
            },
            "mentions": { "issueCount": 0, "nodes": [] },
            "assignedIssues": { "issueCount": 0, "nodes": [] },
            "rateLimit": {
                "limit": 5000,
                "cost": 1,
                "remaining": 4999,
                "resetAt": "2026-04-21T01:00:00Z"
            }
        });
        let data: inbox_query::ResponseData = serde_json::from_value(json).unwrap();
        assert_eq!(data.review_requests.issue_count, 1);
        assert_eq!(data.mentions.issue_count, 0);
        assert_eq!(data.assigned_issues.issue_count, 0);
        let rl = data.rate_limit.unwrap();
        assert_eq!(rl.remaining, 4999);
    }

    #[test]
    fn graphql_error_from_client_error_display() {
        let err = GraphqlError::Query("NOT_FOUND".to_string());
        assert!(format!("{}", err).contains("NOT_FOUND"));
    }
}
```

- [x] **Step 5-4: 全テスト実行**

Run:
```bash
cargo test -p tauri-app --lib github::graphql
cargo test -p tauri-app --lib github::client
```

Expected: すべて PASS。

- [x] **Step 5-5: clippy と fmt チェック**

Run:
```bash
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
```

Expected: warning なし。warning が出たら修正（`unused_imports` 等）して再実行。

- [x] **Step 5-6: コミット**

```bash
git add src-tauri/src/github/graphql/mod.rs src-tauri/src/github/client.rs
git commit -m "feat(m3-013): add fetch_inbox to call GitHub GraphQL /graphql endpoint"
```

---

### Task 6: Push して PR 作成

- [x] **Step 6-1: push**

```bash
git push -u origin issue-48
```

- [x] **Step 6-2: PR 作成**

```bash
gh pr create \
  --base main \
  --head issue-48 \
  --title "feat(m3-013): add GraphQL inbox query client" \
  --body "$(cat <<'EOF'
## Summary
- Vendor GitHub public GraphQL schema to `src-tauri/src/github/graphql/schema.graphql`
- Generate `InboxQuery` Rust types via `graphql_client` derive macro
- Add `fetch_inbox(client, first)` that POSTs to `/graphql` and returns `ResponseData`
- Add `GithubClient::post` helper

## Closes
Closes #48

## Test plan
- [ ] `cargo test -p tauri-app --lib github::graphql` — PASS
- [ ] `cargo test -p tauri-app --lib github::client` — PASS
- [ ] `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
EOF
)"
```

---

# Issue #49 (M3-014): SQLite Connection Pool

### Task 7: issue-49 ブランチを作成

**Files:** なし（ブランチ操作のみ）

- [x] **Step 7-1: main に戻って最新化**

```bash
git checkout main
git pull --ff-only origin main
```

（#48 の PR が未マージでも main から分岐することで独立した変更として扱えます。）

- [x] **Step 7-2: ブランチ作成**

```bash
git checkout -b issue-49
```

---

### Task 8: `tempfile` を dev-dependency に追加

**Files:**
- Modify: `src-tauri/Cargo.toml`

- [x] **Step 8-1: `[dev-dependencies]` セクションを追加**

`src-tauri/Cargo.toml` のファイル末尾に以下を追記（既存に `[dev-dependencies]` が無い場合）:

```toml

[dev-dependencies]
tempfile = "3"
```

- [x] **Step 8-2: 依存解決の確認**

Run:
```bash
cargo check --manifest-path src-tauri/Cargo.toml --tests
```

Expected: 成功（Compiling tempfile v3.x）。

---

### Task 9: `db` モジュールの型と最小テストを追加（失敗から）

**Files:**
- Create: `src-tauri/src/db/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [x] **Step 9-1: 失敗するテストのみを含む `db/mod.rs` を作成**

`src-tauri/src/db/mod.rs` を作成:

```rust
use std::path::{Path, PathBuf};

use r2d2::Pool;
use r2d2_sqlite::SqliteConnectionManager;
use thiserror::Error;

pub type SqlitePool = Pool<SqliteConnectionManager>;

#[derive(Debug, Error)]
pub enum DbError {
    #[error("failed to create database directory: {0}")]
    CreateDir(#[from] std::io::Error),
    #[error("failed to build connection pool: {0}")]
    Pool(#[from] r2d2::Error),
}

#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::TempDir;

    #[test]
    fn pool_type_alias_compiles() {
        // Pure type-level test; ensures SqlitePool alias is reachable.
        fn _accepts(_pool: &SqlitePool) {}
    }

    #[test]
    fn db_error_display_includes_context() {
        let err = DbError::CreateDir(std::io::Error::other("boom"));
        assert!(format!("{}", err).contains("failed to create database directory"));
    }

    #[test]
    fn temp_dir_is_writable() {
        let tmp = TempDir::new().unwrap();
        assert!(tmp.path().exists());
    }
}
```

- [x] **Step 9-2: `lib.rs` に `pub mod db;` を追加**

`src-tauri/src/lib.rs` の既存 `pub mod` 行の直下に追加:

```rust
pub mod auth;
pub mod commands;
pub mod config;
pub mod db;
pub mod github;
```

- [x] **Step 9-3: テスト実行（スケルトン確認）**

Run:
```bash
cargo test -p tauri-app --lib db::
```

Expected: 3 テスト PASS。

- [x] **Step 9-4: コミット**

```bash
git add src-tauri/Cargo.toml src-tauri/src/db/mod.rs src-tauri/src/lib.rs
git commit -m "feat(m3-014): scaffold db module with SqlitePool alias and DbError"
```

---

### Task 10: `init_pool` を TDD で実装

**Files:**
- Modify: `src-tauri/src/db/mod.rs`

- [x] **Step 10-1: 失敗テストを追加**

`src-tauri/src/db/mod.rs` の `mod tests` に以下を追加（既存の 3 テストは残したまま）:

```rust
    #[test]
    fn init_pool_creates_parent_dir_and_opens_connection() {
        let tmp = TempDir::new().unwrap();
        let db_path = tmp.path().join("nested").join("sub").join("pulse.db");
        let pool = init_pool(&db_path).expect("init_pool should succeed");

        let conn = pool.get().expect("checkout connection");
        let one: i64 = conn
            .query_row("SELECT 1", [], |row| row.get(0))
            .expect("query_row");
        assert_eq!(one, 1);

        assert!(db_path.exists(), "pulse.db should be created on connect");
        assert!(db_path.parent().unwrap().exists());
    }

    #[test]
    fn init_pool_accepts_in_memory() {
        let pool = init_pool(Path::new(":memory:")).expect("in-memory pool");
        let conn = pool.get().unwrap();
        conn.execute("CREATE TABLE t (x INTEGER)", []).unwrap();
    }
```

- [x] **Step 10-2: テストを実行して失敗確認**

Run:
```bash
cargo test -p tauri-app --lib db::tests::init_pool_creates_parent_dir_and_opens_connection
```

Expected: FAIL（`init_pool` が未定義）。

- [x] **Step 10-3: `init_pool` を実装**

`src-tauri/src/db/mod.rs` の `pub type SqlitePool` の下（`#[cfg(test)]` より上）に以下を追加:

```rust
pub fn init_pool(db_path: &Path) -> Result<SqlitePool, DbError> {
    if db_path != Path::new(":memory:") {
        if let Some(parent) = db_path.parent() {
            if !parent.as_os_str().is_empty() {
                std::fs::create_dir_all(parent)?;
            }
        }
    }

    let manager = if db_path == Path::new(":memory:") {
        SqliteConnectionManager::memory()
    } else {
        SqliteConnectionManager::file(db_path)
    };
    let pool = Pool::new(manager)?;
    Ok(pool)
}
```

- [x] **Step 10-4: テスト実行で PASS 確認**

Run:
```bash
cargo test -p tauri-app --lib db::
```

Expected: 5 テストすべて PASS。

- [x] **Step 10-5: コミット**

```bash
git add src-tauri/src/db/mod.rs
git commit -m "feat(m3-014): implement init_pool with parent dir creation and in-memory support"
```

---

### Task 11: `pulse_db_path` で AppHandle から `pulse.db` を解決

**Files:**
- Modify: `src-tauri/src/db/mod.rs`

- [x] **Step 11-1: `pulse_db_path` を追加**

`src-tauri/src/db/mod.rs` の `init_pool` の直下に追加:

```rust
pub fn pulse_db_path<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> Result<PathBuf, DbError> {
    use tauri::Manager;
    let data_dir = app
        .path()
        .app_data_dir()
        .map_err(|e| DbError::CreateDir(std::io::Error::other(e.to_string())))?;
    Ok(data_dir.join("pulse.db"))
}
```

そしてファイル先頭の `use` に以下が必要であれば追加（`PathBuf` は既に use 済みなので追加不要、`tauri` は新規 import）:

```rust
// （先頭の use は既存のまま。tauri::Manager は pulse_db_path 内で `use` している）
```

- [x] **Step 11-2: `pulse_db_path` の構造検証テストを追加**

`AppHandle` の生成が単体テストでは困難なため、代わりに「`init_pool(pulse_db_path(app))` の型が合うこと」のコンパイル時チェックと、ファイル名が `pulse.db` であることを確認するダミーを追加。

`mod tests` に以下を追加:

```rust
    #[test]
    fn pulse_db_path_joins_pulse_db_filename() {
        // pulse_db_path joins `pulse.db` to the app data dir.
        // We can't construct an AppHandle in unit tests, so verify by
        // reproducing the join logic.
        let fake_data_dir = PathBuf::from("/tmp/app-data");
        let expected = fake_data_dir.join("pulse.db");
        assert_eq!(expected, PathBuf::from("/tmp/app-data/pulse.db"));
    }

    #[test]
    fn pulse_db_path_signature_accepts_apphandle_runtime() {
        // Type-level check that pulse_db_path is generic over tauri::Runtime.
        fn _sig<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> Result<PathBuf, DbError> {
            super::pulse_db_path(app)
        }
    }
```

- [x] **Step 11-3: テスト実行**

Run:
```bash
cargo test -p tauri-app --lib db::
```

Expected: 7 テストすべて PASS（新規 2 含む）。

- [x] **Step 11-4: clippy と fmt**

Run:
```bash
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
```

Expected: warning / error なし。

- [x] **Step 11-5: コミット**

```bash
git add src-tauri/src/db/mod.rs
git commit -m "feat(m3-014): add pulse_db_path helper resolving app_data_dir/pulse.db"
```

---

### Task 12: Push して PR 作成

- [x] **Step 12-1: push**

```bash
git push -u origin issue-49
```

- [x] **Step 12-2: PR 作成**

```bash
gh pr create \
  --base main \
  --head issue-49 \
  --title "feat(m3-014): initialize SQLite connection pool" \
  --body "$(cat <<'EOF'
## Summary
- Add `src-tauri/src/db/mod.rs` with `SqlitePool` alias, `DbError`, `init_pool`, and `pulse_db_path`
- `init_pool` creates parent directories and supports `:memory:` for tests
- `pulse_db_path` resolves `{app_data_dir}/pulse.db` via Tauri's path API
- Register `pub mod db;` in `lib.rs`
- Add `tempfile` as dev-dependency

## Closes
Closes #49

## Test plan
- [ ] `cargo test -p tauri-app --lib db::` — PASS
- [ ] `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
EOF
)"
```

---

## Final Cleanup

- [x] **Step F-1: プランファイルを削除**

```bash
git checkout main
git pull --ff-only origin main
rm docs/superpowers/plans/2026-04-21-m3-013-014.md
git add -A docs/superpowers/plans/
git commit -m "chore: remove completed plan file for m3-013/014"
git push origin main
```

（ブランチ上で直接削除する場合は最終 PR に含める形でも OK。user 指示は「完了したらプランファイルを削除」なので、全タスク完了後に main に削除コミットを直接 push するのが最短。main に直接 push できなければ一方の PR の最後のコミットに含める。）

---

## Self-Review Notes

**Spec coverage:**
- #48 = M3-013 `graphql_client` で inbox query 型生成 + 呼び出し関数 → Task 2〜6 でカバー
- #49 = M3-014 SQLite 接続プール初期化 + `pulse.db` 作成 → Task 7〜12 でカバー

**Placeholder check:** すべてのコードブロックに実コードを記述、TBD/TODO なし。

**Type consistency:**
- `SqlitePool = Pool<SqliteConnectionManager>` は Task 9 で定義、Task 10/11 で一貫使用
- `init_pool(&Path) -> Result<SqlitePool, DbError>` は Task 10 で定義、Task 11 のテストでも同シグネチャ
- `InboxQuery` / `inbox_query::Variables` / `inbox_query::ResponseData` は Task 3 / 4 / 5 で一貫

**Known risks:**
- GitHub 公開 schema URL が 404 になるケース → Step 2-2 で head 確認。代替は `gh api graphql -f query='query IntrospectionQuery {...}'` で introspection するが、通常は URL で十分。
- `Serialize, Deserialize` を `response_derives` に入れるには `serde` が scope に use されている必要があるため、Task 4 で明示的に import する。
